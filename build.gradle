plugins {
    id 'java'
    id 'maven-publish'
}

group 'com.bimoid.obimp'
version '0.1.0-alpha'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.8.2'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.8.2'
}

test {
    useJUnitPlatform()
}

publishing {
    repositories {
        maven {
            name = 'GithubPackages'
            url = uri('https://maven.pkg.github.com/obimp/obimp4j')
            credentials {
                username = project.findProject('gpr.user')
                password = project.findProperty('gpr.key')
            }
        }
    }
    publications {
        gpr(MavenPublication) {
            from(components.java)
        }
    }
}